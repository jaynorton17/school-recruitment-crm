import React from 'react';

const PinFlow: React.FC = () => {
  // This component is currently unused and has been cleared to resolve a potential build issue.
  return null;
};

export default PinFlow;